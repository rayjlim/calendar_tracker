{
    "watch": {
        "test": {
            "patterns": [
                "{app,bootstrap,database,routes,storage,tests}/*"
            ],
            "extensions": "php"
        }
    },
    "scripts": {
        "test": "vendor\\bin\\phpunit -v",
        "watch": "npm-watch"
    },
    "devDependencies": {
        "npm-watch": "^0.11.0"
    }

}
